{
  "name": "ai-salt",
  "version": "0.1.0",
  "description": "How much do you swear at your coding agents?",
  "license": "MIT",
  "type": "module",
  "bin": {
    "ai-salt": "bin/ai-salt.mjs"
  },
  "files": [
    "bin"
  ],
  "scripts": {
    "build": "bun build --compile src/main.ts --outfile build/salt",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@clack/prompts": "^0.11.0",
    "smol-toml": "^1.3.0"
  },
  "devDependencies": {
    "@salt/core": "workspace:*",
    "@types/bun": "^1.2.0",
    "typescript": "^5.7.0"
  }
}
